# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

# Данные

Архитектура приложения наглядно проиллюстрирована в файле "./src/project architecture.jpg"

Каталог товаров на главной странице состоит из карточек товаров доступных для просмотра, выбора и сохранения в корзину преставлен массивом объектов со следующим интерфейсом:

interface IProduct {
    id: string;
    description: string;
    image: string;
    title: string;
    category: string;
    price: number | null;
}

При оплате заказа покупатель запоняет свои данные в форму, которые потом сохраняются в объект со следующим интерфейсом:

interface IBuyer {
    payment: TPayment;
    email: string;
    phone: string;
    address: string
}

postApi - тип для отправки данных на сервер

type postApi = IBuyer & {
    total: number,
    items: string[]
}

-----------------------------------------

Класс Каталог товаров - Каталог товаров на главной странице

class MainCatalog 

Products : IProduct[]
SelectedProduct : IProduct

---methods---

getProducts - получить список всех товаров на главной странице
Возвращает : IProduct[]

setProducts - сохранить список всех товаров : IProduct[]

getSelectedProduct - получить выбранную пользователем карточку товара
Возвращает карточку товара: IProduct

setSelectedProduct - сохранить выбранную пользователем карточку товара : IProduct

------------------------------------------

Класс Корзина - хранит выбранные пользователем товары для последующей покупки

class Cart

Cart - массив товаров в корзине : IProduct[]

---methods---

addToCart - добавляет в массив товаров в корзине новый товар
Принимает выбранный товар : IProduct
Возвращает новый массив товаров в корзине
: IProducts[]

removeFromCart - удаляет товар из массива в корзине
Принимает выбранный товар : IProduct
Возвращает новый массив товаров в корзине
: IProducts[]

getQuantity - считает количество элементов в массиве товаров в корзине
Принимает массив товаров в корзине : IProducts[]
Возвращает : number

getProductsFromCart - получить список товаров в корзине
Возвращает : IProduct[]

calculateTotalPrice - считает сумму стоимоти всех товаров в корзине
Принимает массив товаров в корзине : IProducts[]
Возвращает : number

checkCart - проверяет есть ли товар уже в корзине
принимает IProduct.id ?
возвращает true | false

--------------------------------------------

Класс Покупатель - хранит и обрабатывает данные заполненные покупателем при оформлении заказа

class Buyer

Payment method: card | cash | null
Address: string
Email: string
Phone number: string

---methods---

getData - получить данные заполненные пользователем
возвращает : IBuyer

validateData - проверяет данные и возвращает результат проверки
Принимает заполненные данные : IBuyer
Возвращает результат проверки 

clearData - удаляет данные покупателя

saveData - сохраняет данные покупателя
: IBuyer

---------------------------------------------

Класс WebLarekApi - получает данные о таворах с сервера и отправляет данные покупателя на сервер

class WebLarekApi 

baseLink: string 

getProducts - получает данные с сервера  
возвращает массив с данными

postOrder - отправляет на сервер данные покупателя

---------------------------------------------

Классы слоя представления 

---------------------------------------------

Класс CartView - отображает интерфейс корзины и вмещает в себя товары добавленые в корзину

Итерфейс корзины (данные для отображения):
interface ICart {
    totalPrice: number; - общая стоимость всех товаров в корзине
    products: HTMLElement[] | HTMLElement | null; - товары в корзине
}

cartTotal: HTMLElement; - элемент для отображения общей стоимости товаров в корзине
productsCart: HTMLElement;  - элемент куда будут добавлятся товары
cartOrderButton: HTMLButtonElement; - кнопка оформления заказа

---methods---

set totalPrice 
задает общая стоимость всех товаров в корзине для отображения

set products
задает список товаров для отображения

disableButton() - активирует/деактивирует кнопку если корзина пуста

---------------------------------------------

Класс Gallery - галерея которая вмещает карточки товаров для отображения

Интерфейс 
interface IGallery {
    content: HTMLElement[]; - карточки товаров
}


gallery: HTMLElement; - контейнер для карточек

---methods---

set content - отобразить карточки товаров 

---------------------------------------------

Класс Header - шапка страницы с кнопкой корзины и счетчиком товаров

interface IHeader {
    counter: number - счетчик товаров в корзине 
}


counterElement: HTMLElement; - элемент для отображения счетчика
cartButton: HTMLButtonElement; - кнопка корзины

---methods---

set counter - отобразит значения счетчика

---------------------------------------------

Класс ModalWindow - модальное окно в котором отображается различный контент 

interface IModal {
    content: HTMLElement - контент модального окна
}

modalCloseButton: HTMLButtonElement; - кнопка закрытия модального окна
contentModal: HTMLElement; - контейнер для контента

---methods---

set content - отобразить контент модаьного окна

show() - показать модальное окно

close() - скрыть модальное окно

---------------------------------------------

Класс Form - родитель для классов OrderForm и ContactsForm в который вынесен общий функционал

interface IForm {
    formErrors: string | null; - сообщения валидации
}

errors: HTMLElement; - элемент для отображения ошибок
submitButton: HTMLButtonElement; - кнопка оформления заказа

---methods---

disableButton() - активирует/деактивирует кнопку если присутствуют ошибки в форме

---------------------------------------------

Класс OrderForm - потомок формы с тем же интерфейсом

paymentCardButton: HTMLButtonElement; - кнопка выбора оплаты картой
paymentsCashButton: HTMLButtonElement; - кнопка выбора оплаты наличкой
inputAddressField: HTMLInputElement; - элемент ввода адреса

---------------------------------------------

Класс ContactsForm - потомок формы с тем же интерфейсом

inputEmailField: HTMLInputElement; - элемент ввода емайла
inputPhoneField: HTMLInputElement; - элемент ввода телефона

---------------------------------------------

Класс Product - общий родитель для всех классов отображения товаров в который вынесен общий функционал

interface ICardActions {
    onClick?: () => void; - обработчик собития клика 
}

productTitle: HTMLHeadingElement; - элемент для отображения названия
productPrice: HTMLElement; - элемент для отображения цены 

---methods---

set title - отобразить название товара 

set price - отобразить уены товара 

---------------------------------------------

Класс ProductGalleryView - отображает карточки в галлере на главной стнанице

productOpenCardButton: HTMLButtonElement; - карточка-кнопка открытия модального окна
productCategory: HTMLElement; - элемент для отображения категории товара
productImage: HTMLImageElement; - элемент для отображения картинки товара 

---methods---

set category - отобразить категорию товара 

set imageLink - задать путь к картинке товара 

set imageDescription - задать описание картинки

---------------------------------------------

Класс ProductPreview - детальное описание товара которое отображается в модальном окне

productCategory: HTMLElement; - элемент для отображения категории товара
productImage: HTMLImageElement; - элемент для отображения картинки товара
productDescription: HTMLElement; - элемент для отображения описания товара
productAddRemoveButton: HTMLButtonElement; - кнопка для добавления или удаления из корзины

---methods---

set category - отобразить категорию товара 

set imageLink - задать путь к картинке товара 

set imageDescription - задать описание картинки

set description - задать описание товара 

set button - задать текст на кнопке

disableButton() - активирует/деактивирует кнопку если товар недоступен

---------------------------------------------

Класс ProductCartView - отображение товара в корзине 

productIndex: HTMLElement; - элемент для отображения нумерации товара 
productRemoveButton: HTMLButtonElement; - кнопка для удаления товара из корзины 

---methods---

set index - задать порядковый номер товара 

---------------------------------------------

Класс SuccessModal - отображает контент модального окна при успешном оформлении заказа

interface ISuccess {
    totalPrice: number; - общая сумма заказа
}

returnButton: HTMLButtonElement; - кнапка закрытия модального окна
orderTotal: HTMLElement; - элемент для отображения общей суммы заказа 

---methods---

set totalPrice - отобразить сумму заказа

---------------------------------------------

СОБЫТИЯ

---------------------------------------------

События изменения в моделях данный:

    catalog changed - изменения списка товаров на главной странице => перерисовать карточки на главной странице
    selected product changed - изменился выбранный товар => отрисовать разметку карточки с подробнымм описанием товара 
    cart content change - измелся список товаров в корзине => отрисовать заного товары в корзине 
    order change - изменения введеных пользователем данных в форме Order => валидация полей, отображения ошибок, активация/деактивация кнопки

    
    contacts change - изменения введеных пользователем данных в форме Contacts => валидация полей, отображения ошибок, активация/деактивация кнопки
    product:open - пользователь кликает по карточке товара в главном каталоге => открывается модальное окно с выбранным товаром
    cart button click - пользователь кликает по кнопке дабавить/удалить из корзины товар => удалить/добаить товар в модели данных корзины, закрыть модальное окно 
    cart:open - пользователь кливает по иконке корзины => открывается модальное окна с содержимым корзины 
    cart: remove product - пользователь кликает по кнопке удалить товар из корзины => товар удаляется из модели данных корзины
    cart: order - пользователь кликет по кнопке оформить заказ в корзине => открывается модальное окно заказа
    payment:card - пользователь кликает по кнопке оплата картой => оплата картой сохраняется в модели данных покупателя
    payment:cash - пользователь кликает по кнопке оплата наличкой => оплата наличкой сохраняется в модели данных покупателя
    address: changed - пользователь меняет введенные данные в строке адреса => адрес сохраняется в модель данных пользователя  
    email: changed - пользователь меняет введенные данные в строке емайла => емайл сохраняется в модель данных пользователя
    phone: changed - пользователь меняет введенные данные в строке телефона => номер телефона сохраняется в модель данных пользователя
    order:submit - пользователь нажимает далее в форме заказа => в модальном окне отображается форма контактов
    contacts:submit - пользователь наживает отправить заказ в форме контактов => формирует данные заказа (данные покупателя из формы, общая стоимость заказа, список товаров), запрос с заказом отправляется на сервер, отображается модальное окна с сообщением о успешной покупке, очищаются данные о покупателе, очищается корзина 
    